# API Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# LLM — OpenAI SDK (works with Ollama local server too)
openai==1.57.0

# Database — SQLite for dev, swap URL for PostgreSQL in prod
sqlalchemy==2.0.36
aiosqlite==0.20.0        # async SQLite driver
alembic==1.14.0          # migrations

# Validation & Settings
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# HTTP Client (portal scraping)
httpx==0.27.2
beautifulsoup4==4.12.3

# PDF Generation (one-pager, cover letter)
jinja2==3.1.4
weasyprint==62.3

# Browser Automation (Apply Agent)
playwright==1.49.0

# Background Tasks
celery==5.4.0
redis==5.2.1

# Utilities
tenacity==9.0.0          # retry logic for LLM calls
structlog==24.4.0        # structured logging
python-slugify==8.0.4
